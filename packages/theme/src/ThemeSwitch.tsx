import * as React from 'react';
import { ThemeButton, ThemeIcon } from '.';
import '../styles/components';

export default function ThemeSwitch(): JSX.Element {
  return (
    <ThemeButton>
      <ThemeIcon />
    </ThemeButton>
  );
}
